document.querySelector('button').addEventListener('click', () => {
  const code = document.querySelector('textarea').value;
  alert('Code submitted!');
  // Add logic to validate code and award points
});